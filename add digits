int addDigits(int num) {
    int sum=0,rem,temp;
    temp=num;
    while(1){
    while(temp){
        rem=temp%10;
        sum+=rem;
        temp/=10;
    }
    if(sum<10){
        return sum;
    }
    else{
        temp=sum;
        sum=0;
    }
    }
}
